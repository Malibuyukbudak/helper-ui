"use client";

import { createTheme } from "@mui/material/styles";

const palette = {
  primary: {
    main: "#1877F2",
  },
};
const theme = createTheme({
  palette,
  components: {},
});

export default theme;
