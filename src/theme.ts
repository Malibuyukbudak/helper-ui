"use client";

import { createTheme } from "@mui/material/styles";

const palette = {
  primary: {
    main: "#5580B2",
  },
};
const theme = createTheme({
  palette,
  components: {},
});

export default theme;
