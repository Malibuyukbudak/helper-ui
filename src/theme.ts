"use client";

import { createTheme } from "@mui/material/styles";

const palette = {
  primary: {
    main: "#FF6274",
  },
  inherit: {
    main: "#FFFFFF",
  },
};
const theme = createTheme({
  palette,
  components: {},
});

export default theme;
